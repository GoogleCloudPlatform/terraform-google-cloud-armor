# Upgrading to v8.0.0

The v8.0 release contains backwards-incompatible changes.

### TPG min version required
Min provider version is bumped to `7.17` for global backend security policy module and `6.49` for regional backend security policy, network edge security policy and advance network ddos protection modules.

